= form_for @licitacion_imagen do |f|
  - if @licitacion_imagen.errors.any?
    #error_explanation
      %h2= "#{pluralize(@licitacion_imagen.errors.count, "error")} prohibited this licitacion_imagen from being saved:"
      %ul
        - @licitacion_imagen.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :descripcion
    = f.text_field :descripcion
  .field
    = f.label :imagen
    = f.text_field :imagen
  .field
    = f.label :producto_id
    = f.number_field :producto_id
  .field
    = f.label :producto_token
    = f.text_field :producto_token
  .actions
    = f.submit 'Save'
